{
  "name": "api",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "generate": "drizzle-kit generate",
    "local:migration": "wrangler d1 migrations apply holiday-jp --local",
    "remote:migration": "wrangler d1 migrations apply holiday-jp --remote"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.4.1",
    "@hono/zod-openapi": "^0.16.4",
    "drizzle-orm": "^0.36.0",
    "hono": "^4.6.8",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "drizzle-kit": "^0.27.0",
    "wrangler": "^3.83.0"
  }
}
